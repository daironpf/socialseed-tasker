FROM python:3.11-slim
WORKDIR /app
COPY . /app
RUN python -m pip install --upgrade pip && pip install -e . ariadne uvicorn
EXPOSE 8081
CMD ["uvicorn", "socialseed_tasker.graphql.server:create_app()", "--host", "0.0.0.0", "--port", "8081"]
